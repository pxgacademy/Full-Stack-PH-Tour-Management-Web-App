export interface iTourType {
  name: string;
}

export interface iTourTypeResponse {
  name: string;
  _id: string;
  createdAt: string;
  updatedAt: string;
}

export interface iUpdateTourType {
  id: string;
  name: string;
}
