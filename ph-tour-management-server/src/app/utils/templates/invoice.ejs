<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Invoice - <%= bookingId %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 2rem;
        color: #333;
        background-color: #f8f9fa;
      }
      .invoice-container {
        background: #fff;
        padding: 2rem;
        border-radius: 8px;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
      }
      h1 {
        font-size: 1.8rem;
        border-bottom: 1px solid #ccc;
        padding-bottom: 0.5rem;
      }
      .section {
        margin: 1.5rem 0;
      }
      .section p {
        margin: 0.3rem 0;
      }
      .footer {
        text-align: center;
        margin-top: 2rem;
      }
      .download-btn {
        display: inline-block;
        background-color: #007bff;
        color: #fff;
        padding: 0.6rem 1.2rem;
        border-radius: 4px;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="invoice-container">
      <h1>Invoice for Tour Booking</h1>

      <div class="section">
        <p><strong>Customer Name:</strong> <%= name %></p>
        <p><strong>Email:</strong> <%= email %></p>
        <p><strong>Booking ID:</strong> <%= bookingId %></p>
        <p><strong>Tour Title:</strong> <%= tourTitle %></p>
        <p><strong>Date:</strong> <%= bookingDate %></p>
      </div>

      <div class="section">
        <p><strong>Payment ID:</strong> $<%= paymentId %></p>
        <p><strong>Amount Per Guest:</strong> $<%= amountPerGuest %></p>
        <p><strong>Total Guests:</strong> $<%= totalGuests %></p>
        <p><strong>Amount Paid:</strong> $<%= amount %></p>
        <p><strong>Payment Method:</strong> <%= paymentMethod %></p>
        <p><strong>TrxID:</strong> <%= TrxId %></p>
      </div>

      <div class="section">
        <p>Thank you for booking your tour with <strong>PH Tour</strong>!</p>
        <p>We hope you have a great experience.</p>
      </div>

      <!-- <div class="footer">
        <a href="<%= downloadPdfUrl %>" class="download-btn"
          >Download Invoice PDF</a
        >
      </div> -->
    </div>
  </body>
</html>
